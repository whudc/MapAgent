# Python Execution Environment

- ALWAYS run Python commands inside the specified conda environment: `mapagent (D:\program\conda_envs\mapagnet)`
- NEVER use the system default Python
- NEVER assume the environment is already activated
